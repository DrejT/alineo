{
  "name": "@alineo-labs/alineod",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "alineod — the swarm control daemon. Prototype (prototype/alineod branch). See research/daemon.md.",
  "scripts": {
    "start": "bun server.ts",
    "dev": "bun --watch server.ts",
    "typecheck": "tsc --noEmit --project tsconfig.json",
    "spec": "bun scripts/emit-spec.ts"
  },
  "dependencies": {
    "alineo": "workspace:*",
    "@alineo-labs/core": "workspace:*",
    "@alineo-labs/sqlite": "workspace:*",
    "elysia": "^1.4.9",
    "@elysiajs/openapi": "^1.4.9",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "bun-types": "1.3.14",
    "typescript": "6.0.3"
  }
}
