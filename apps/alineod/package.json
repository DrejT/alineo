{
  "name": "@alineo-labs/alineod",
  "version": "0.0.0",
  "private": true,
  "description": "alineod — the swarm control daemon: HTTP + SSE orchestration for swarms of sandboxed agents.",
  "type": "module",
  "scripts": {
    "start": "bun server.ts",
    "dev": "bun --watch server.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit --project tsconfig.json",
    "spec": "bun scripts/emit-spec.ts"
  },
  "dependencies": {
    "@alineo-labs/core": "workspace:*",
    "@alineo-labs/sqlite": "workspace:*",
    "@elysiajs/openapi": "^1.4.9",
    "alineo": "workspace:*",
    "elysia": "^1.4.9",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "bun-types": "1.3.14",
    "typescript": "6.0.3"
  }
}
