---
---

Bump `next` to 16.3.4 in the docs app, closing two critical advisories affecting 16.0.0–16.3.2:
unauthenticated remote code execution in the Image Optimization API, and unauthenticated RCE on
Windows-hosted servers. `apps/docs` is private, so no published package changes and no release
is needed.
