---
"alineo-mcp": patch
---

`alineo-mcp` now reports its real package version in the MCP `initialize` handshake (it was hardcoded to `0.1.0`), and its README no longer says the package is unpublished. Adds a docs page at `/docs/alineod/getting-started/mcp`.
