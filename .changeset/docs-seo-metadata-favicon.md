---
"docs": patch
---

Fix docs favicon to match the alineo brand, add per-page SEO metadata (title/description/canonical/OG images generated per doc page instead of one sitewide default), fix the sitemap missing /changelog and /use-cases, and add WebSite JSON-LD.
