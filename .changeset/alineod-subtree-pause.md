---
---

alineod only (private app), no publishable package changes. Subtree controls and coordination (research/tier-1-2-plan.md): pause / resume / stop take `scope: "subtree"` with per-member results and `pausedBy` provenance; subtree steer delivers one message to the parent with a roster of its children; stopping a finished agent keeps its outcome (`agent_released`); `waitFor` gains `all` / `any` / `quorum` modes and deadlines; quiescence and operator await routes; `notifyOn` with a per-agent inbox.
