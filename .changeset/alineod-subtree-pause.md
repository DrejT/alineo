---
---

alineod only (private app), no publishable package changes: pause and resume take `scope: "subtree"` to act on an agent and every descendant, with per-member results and `pausedBy` provenance.
