---
---

Bump `react` and `react-dom` to 19.3.0 together in the docs app. React requires the two to stay
on the same version, so they move as one change rather than as separate bumps. `apps/docs` is
private, so no published package changes and no release is needed.
